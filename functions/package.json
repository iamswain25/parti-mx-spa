{
  "name": "functions",
  "scripts": {
    "build": "tsc",
    "serve": "npm run build && firebase serve --only functions --project parti-2020",
    "shell": "npm run build && firebase functions:shell --project parti-2020",
    "start": "npm run shell",
    "deploy": "tsc && firebase deploy --only functions --project parti-2020",
    "deploy1": "tsc && firebase deploy --only functions:groupCreate --project parti-2020",
    "logs": "firebase functions:log"
  },
  "engines": {
    "node": "12"
  },
  "main": "lib/index.js",
  "dependencies": {
    "firebase-admin": "^9.2.0",
    "firebase-functions": "^3.11.0",
    "isbot": "^3.0.23",
    "nodemailer": "^6.4.17"
  },
  "devDependencies": {
    "@types/nodemailer": "^6.4.0",
    "firebase-functions-test": "^0.2.0",
    "typescript": "^3.8.0"
  },
  "private": true
}
