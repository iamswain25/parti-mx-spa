{
  "name": "functions",
  "scripts": {
    "lint": "tslint --project tsconfig.json",
    "build": "tsc",
    "serve": "npm run build && firebase serve --only functions --project sharenting-mix",
    "shell": "npm run build && firebase functions:shell",
    "start": "npm run shell",
    "deploy": "tsc && firebase deploy --only functions --project sharenting-mix",
    "deployshare": "tsc && firebase deploy --only functions:post --project sharenting-mix",
    "logs": "firebase functions:log"
  },
  "engines": {
    "node": "10"
  },
  "main": "lib/index.js",
  "dependencies": {
    "@types/firebase": "^3.2.1",
    "@types/node": "^13.13.30",
    "firebase-admin": "^9.3.0",
    "firebase-functions": "^3.11.0",
    "isbot": "^3.0.14"
  },
  "devDependencies": {
    "firebase-functions-test": "^0.1.6",
    "minimist": "^1.2.5",
    "tslint": "^5.12.0",
    "typescript": "^3.9.7"
  },
  "private": true
}
